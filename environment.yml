name: spark_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - openjdk=17
  - notebook
  - jupyterlab
  - pip
  - pip:
      - pyspark>=3.5.0,<4.0.0
      - delta-spark>=3.2.0
      - pyarrow>=15.0.0
      - pandas>=2.2.0
      - pytest>=8.2.0
      - chispa>=0.9.4
      - ruff>=0.5.0
